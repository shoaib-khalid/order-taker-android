package com.symplified.ordertaker.data.repository

import com.symplified.ordertaker.data.ProductDao

class ProductRepository(private val productDao: ProductDao) {
//    val allItems: Flow<List<Product>> = productDao.getAll()
//
//    fun insert(product: Product) = productDao.insert(product)
}